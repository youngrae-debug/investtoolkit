# yfinance market-data service

```bash
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000
```

InvestToolkit에는 배포 URL을 `YFINANCE_API_BASE_URL` 환경변수로 설정합니다.

> yfinance 및 Yahoo Finance 데이터의 이용 조건을 확인한 뒤 개인·연구 목적 범위에서 사용하세요.
